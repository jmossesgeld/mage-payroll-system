import { TimeRecord } from "../../../lib/types";

export default function TimeRecord({
  timeIn,
  timeOut,
  isRestDay,
  isAbsent,
  isRegularHoliday,
  isSpecialHoliday,
}: TimeRecord) {
  return (
    <div>
      <h1>TimeRecord</h1>
    </div>
  );
}
